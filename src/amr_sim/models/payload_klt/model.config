<?xml version="1.0"?>
<model>
  <name>payload_klt</name>
  <version>1.0</version>
  <sdf version="1.10">model.sdf</sdf>
  <description>
    A carried load that rides on the top plate by friction alone, so that
    whether it stays there is a result rather than an assumption. Hand
    authored: no wf_ prefix, so the AWS importer never touches it.
  </description>
</model>
