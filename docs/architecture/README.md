# Architecture documentation

Follows the arc42 template: context and scope, constraints, solution strategy, building block view,
runtime view, deployment view, cross cutting concepts, decisions, quality requirements, risks.

Written as the system takes shape rather than up front. Decisions already made are recorded in
`../adr/` and are referenced from here rather than duplicated.
